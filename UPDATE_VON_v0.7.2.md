# Update von Mampfo v0.7.2 auf v0.7.2.1

1. Die Dateien aus diesem Paket über die vorhandenen App-Dateien im Repository kopieren.
2. Die bestehende `supabase-config.js` im Repository unverändert behalten.
3. Es ist keine Änderung in Supabase erforderlich.
4. GitHub Pages vollständig deployen lassen.
5. Mampfo anschließend hart neu laden bzw. die installierte PWA einmal vollständig schließen und neu öffnen.
6. Unter **Erfassen → Lebensmittel → Produkte** eine Suche testen, z. B. `Alpro Soja`.

Der Patch korrigiert ausschließlich die Open-Food-Facts-Suche und verändert keine vorhandenen Lebensmittel-, Tagebuch-, Rezept- oder Sync-Daten.
